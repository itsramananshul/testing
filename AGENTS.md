# AGENTS.md

## Cursor Cloud specific instructions

This repository is currently empty (only contains a placeholder `README.md`). There are no applications, services, dependencies, or build systems to set up.

- **No package manager** is configured (no `package.json`, `requirements.txt`, `Cargo.toml`, `go.mod`, etc.).
- **No services** need to be started.
- **No lint, test, or build commands** are available.

When application code is added to this repository, this section should be updated with relevant development environment instructions.
